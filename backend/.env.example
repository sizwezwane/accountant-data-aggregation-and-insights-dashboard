# Database Configuration

# Option 1: PostgreSQL (Recommended for production)
DATABASE_URL=postgresql://username:password@localhost:5432/accountant_dashboard

# Option 2: SQLite (Simple, no setup required)
# Uncomment the line below to use SQLite instead
# USE_SQLITE=true

# Note: 
# - For PostgreSQL, make sure the database exists and credentials are correct
# - For SQLite, set USE_SQLITE=true in your .env file
# - The application will automatically create tables on first run
